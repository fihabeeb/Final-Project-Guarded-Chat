# Todo

- Use localstorage to have users autologin
- autogenerate their friendslist
- then autogenerate those friends' chat history
- then check if a peer request has been made from each user, if not, automatically creates a peer request to each user that hasnt made one already




### longterm considerations:

- Consider what happens if person A has person B as a friend, but person B does not have person A as a friend. This ideally should'nt happen, as when you unfriend, it should reflect on both sides. This then means that some sort of server queue is to be made for adding, and removing friends